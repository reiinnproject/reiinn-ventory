# MongoDB connection string (from MongoDB Atlas)
MONGODB_URI=mongodb+srv://user:password@cluster.mongodb.net/reiinn_ventory

# JWT secret for auth tokens (generate a random string)
JWT_SECRET=your-secret-key-here
